local mat = require("colors")
hl.config (

{
    general = {
        layout = "scrolling",
        gaps_out = 7,
        gaps_in = 5,
        border_size = 1,
        col = {
            active_border = mat.primary,
            inactive_border = mat.surface
        }
    }
}

)